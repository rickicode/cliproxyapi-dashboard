RED PHASE TEST RESULTS - Slim-Variant Agent Skip Bug
======================================================

TEST FILE: dashboard/src/lib/__tests__/slim-config-chains.test.ts

NEW TEST ADDED (RED - FAILING):
  ✗ buildSlimConfig – agent inclusion when models unavailable
    ✗ should include all 6 agents even when no models match any tier
       Error: expected null not to be null
       Line: 63 in slim-config-chains.test.ts

EXISTING TESTS (PASSING):
  ✓ buildSlimConfig – fallback chains bug reproduction
    ✓ should not emit empty chain arrays for agents where all models are unavailable
    ✓ should omit chains entirely when all chains resolve to empty

SUMMARY:
--------
Test Count:     3 total (2 existing + 1 new)
Passing:        2 (existing tests still pass)
Failing:        1 (new test correctly exposes the bug)
Status:         RED PHASE CONFIRMED

BUG DETAILS:
-----------
Location: dashboard/src/lib/config-generators/oh-my-opencode-slim.ts, line 50
Code:     if (!model) continue;

When buildSlimConfig([]) is called with empty available models:
1. pickBestModel([], tier) returns null for all tiers
2. The continue statement skips adding each agent
3. agents object ends up empty
4. Line 62-64: if (Object.keys(agents).length === 0) return null;
5. buildSlimConfig([]) returns null

Expected fix:
All 6 agents (orchestrator, oracle, designer, explorer, librarian, fixer)
should still be included in the output even when no models match their tier.

NEXT PHASE: GREEN - Implement fix in oh-my-opencode-slim.ts
